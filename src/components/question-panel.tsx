export function QuestionPanel({
  workspaceId,
  roundNumber,
  onComplete,
}: {
  workspaceId: string;
  roundNumber: number;
  onComplete: () => void;
}) {
  void workspaceId; void roundNumber; void onComplete;
  return null;
}
